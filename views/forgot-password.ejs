<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password | BookHeaven</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            background: #f0f2f5;
            font-family: "Segoe UI", Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .auth-card {
            background: #fff;
            width: 380px;
            padding: 30px 25px;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.08);
            text-align: center;
        }

        .auth-card h1 {
            font-size: 26px;
            margin-bottom: 10px;
            font-weight: 700;
            color: #333;
        }

        .auth-card p {
            font-size: 14px;
            color: #555;
            margin-bottom: 25px;
        }

        .auth-card input {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: 1px solid #c9c9c9;
            margin-bottom: 20px;
            font-size: 15px;
        }

        .auth-card input:focus {
            outline: none;
            border-color: #6a5acd;
            box-shadow: 0 0 4px rgba(106, 90, 205, 0.3);
        }

        .auth-card button {
            width: 100%;
            padding: 12px;
            background: #6a5acd;     /* BookHeaven purple */
            border: none;
            color: white;
            font-size: 16px;
            font-weight: 600;
            border-radius: 8px;
            cursor: pointer;
            transition: 0.2s;
        }

        .auth-card button:hover {
            background: #584dc0;
        }

        .back-link {
            margin-top: 15px;
            display: block;
            font-size: 14px;
            color: #6a5acd;
            text-decoration: none;
        }

        .back-link:hover {
            text-decoration: underline;
        }
    </style>

</head>
<body>

    <div class="auth-card">
        <h1>Forgot Password</h1>
        <p>Enter your Gmail to receive a reset link.</p>

        <form action="/forgot-password" method="POST">

            <input 
                type="email" 
                name="email" 
                placeholder="Enter your Gmail"
                required
            >

            <button type="submit">Send Reset Link</button>
        </form>

        <a href="/login" class="back-link">Back to Login</a>
    </div>

</body>
</html>
