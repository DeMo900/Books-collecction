<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>All Books | Book Haven</title>
  <style>
    .hero {
  text-align: center;
  margin: 100px auto;
  padding: 50px 20px;
  background: linear-gradient(135deg, rgba(255,111,145,0.1), rgba(255,148,114,0.1));
  border-radius: 25px;
  width: 80%;
  max-width: 600px;
  box-shadow: 0 0 20px rgba(0,0,0,0.2);
  backdrop-filter: blur(5px);
  animation: fadeIn 1s ease forwards;
}

.hero p {
  font-size: 1.5rem;
  font-weight: 600;
  letter-spacing: 1px;

  /* fallback color for non-supporting browsers */
  color: #fff;

  /* gradient background (visible through text when clipped) */
  background: linear-gradient(135deg, #ff6f91, #ff9472);

  /* make the gradient show through text in WebKit browsers */
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;

  /* try unprefixed as well (may be flagged by some validators) */
  background-clip: text;
  color: transparent; /* fallback for browsers honoring background-clip:text */

  animation: floatText 2.5s ease-in-out infinite;
}


@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes floatText {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-8px);
  }
}

    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap');

    body {
      font-family: "Poppins", sans-serif;
      margin: 0;
      background: linear-gradient(135deg, #1f1c2c, #928dab);
      color: #f2f2f2;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 60px;
      background: rgba(25, 25, 35, 0.85);
      backdrop-filter: blur(10px);
      position: sticky;
      top: 0;
      z-index: 10;
    }

    header h1 {
      font-size: 1.8rem;
      letter-spacing: 1px;
    }

    nav a {
      color: #eee;
      margin-left: 25px;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    nav a:hover {
      color: #ff6f91;
    }

    .search-bar {
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 50px auto 20px;
      width: 80%;
      max-width: 600px;
      position: relative;
    }

    .search-bar input {
      width: 100%;
      padding: 14px 55px 14px 20px;
      border: none;
      border-radius: 30px;
      outline: none;
      font-size: 1rem;
      color: #333;
      background: #fff;
      transition: all 0.3s ease;
    }

    .search-bar input:focus {
      box-shadow: 0 0 10px rgba(255, 111, 145, 0.5);
      transform: scale(1.02);
    }

    .search-bar button {
      position: absolute;
      right: 8px;
      top: 50%;
      transform: translateY(-50%);
      background: linear-gradient(135deg, #ff6f91, #ff9472);
      border: none;
      border-radius: 30px;
      padding: 10px 18px;
      color: #fff;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .search-bar button:hover {
      background: linear-gradient(135deg, #ff9472, #ff6f91);
      transform: translateY(-50%) scale(1.05);
      box-shadow: 0 0 10px rgba(255, 111, 145, 0.5);
    }

    .book-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
      gap: 25px;
      padding: 50px 60px;
    }

    .book-card {
      background: rgba(255,255,255,0.1);
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 4px 20px rgba(0,0,0,0.3);
      transition: all 0.3s ease;
    }

    .book-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 6px 25px rgba(0,0,0,0.5);
    }

    .book-card img {
      width: 100%;
      height: 330px;
      object-fit: cover;
    }

    .book-info {
      padding: 15px 20px;
    }

    .book-info h3 {
      font-size: 1.1rem;
      margin: 5px 0;
      color: #fff;
    }

    .book-info p {
      font-size: 0.9rem;
      margin: 5px 0;
      color: #ddd;
    }

    .genre-tag {
      display: inline-block;
      background: linear-gradient(135deg, #ff6f91, #ff9472);
      color: #fff;
      border-radius: 15px;
      padding: 5px 12px;
      font-size: 0.8rem;
      margin-top: 8px;
    }

    footer {
      text-align: center;
      padding: 25px;
      background: #14141e;
      border-top: 1px solid rgba(255,255,255,0.1);
    }

    footer p {
      margin: 0;
      color: #aaa;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>
  <header>
    <h1>📚 Book Haven</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/books">All Books</a>
      <a href="/about">About</a>
    </nav>
  </header>

  <main>
    <form class="search-bar" id="form" action="/books/search" method="POST">
      <input id="search" type="text" value = "<%=query%>" name="query" placeholder="Search for a book or genre..." required />
      <button type="submit">Search</button>
    </form>
  <section class="book-grid">
<%if(data.length===0) {%>
  <section class="hero">
<p> No results &#128542 </p>
</section>
  <%}%>
    <section class="book-container">
      <% data.forEach(el => { %>
        <div class="book-card">
          <img src="<%= el.coverurl %>" alt="Book Cover" />
          <div class="book-info">
            <h3><%= el.title %></h3>
            <p><%= el.author %></p>
            <p><%= el.publishyear %></p>
            <span class="genre-tag"><%= el.genre %></span>
          </div>
        </div>
      <% }) %>
    </section>
  </main>

  <footer>
    <p>© 2025 Book Haven. All rights reserved.</p>
  </footer>
 <script src="/script.js"></script>
</body>
</html>
